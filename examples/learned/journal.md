# Learning Journal — example

> Demo of how the journal accrues across runs. Append-only; one dated entry per run.
> In a real vault this lives at `.agents/learned/journal.md`.

## 2026-03-01 — Positional Encoding (topic build)

- **Input:** `Inbox/positional-encoding-brief.md` — a short topic brief.
- **Classified as:** Topic Build → `ML/Positional Encoding/`
- **Corrections:** none (initial run)
- **Fixes:** none
- **Lesson:** none this run

## 2026-03-02 — Positional Encoding correction

- **Input:** user review of the previous run
- **Classified as:** n/a (review)
- **Corrections:** User moved the `Positional Encoding` concept note out of the
  collection-local `References/` and into the **vault-root** `References/`, then
  linked to it: "Positional encoding is foundational and reused across ML
  collections — it belongs in the shared References/, not duplicated per topic."
- **Fixes:** Rewired the collection's wikilinks to point at `References/Positional Encoding`.
- **Lesson:** Foundational, cross-cutting ML concepts (positional encoding, attention,
  embeddings, normalization, optimizers) go in the vault-root `References/`; collections
  link to them instead of creating local copies.
